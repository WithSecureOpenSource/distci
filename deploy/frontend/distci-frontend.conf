{
    "data_directory": "/var/lib/distci/frontend",
    "zookeeper_nodes": [
#for $c, ($zknode) in enumerate($find("distci/zookeeper/zknode"))
${",\n" if $c else ""}"$zknode.private.dns:2181"#slurp
#end for
    ]
}
